package fractionCalculator;

public class Calculator {
    static Fraction add (Fraction f1, Fraction f2) {
//        Fraction summa = new Fraction ();
//        summa.denominator = f1.dedenominator + f2.denominator;
//        summa.numerator = f1.numerator * f2.numerator + f1.dedenominator * f2.denominator;
//        return summa;
        return null;
    }
}
